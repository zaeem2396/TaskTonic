meta {
  name: Gemini
  type: http
  seq: 3
}

get {
  url: http://localhost:4000/api/v1/gemini
  body: json
  auth: none
}

body:json {
  {
    "message" : "create a join query between users and task table to fetch which task is assigned to which user, reference columns are from users table is id and tasks table is assignedTo"
  }
}
